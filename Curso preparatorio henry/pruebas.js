function tieneTresDigitos(num) {
    // Si el número recibido tiene tres dígitos retornar true.
    // Caso contrario, retornar false.
    // Tu código:
    str = num.toString();
    console.log(str);
 }

 tieneTresDigitos(1231)